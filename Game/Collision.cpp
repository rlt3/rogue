#include <Game/Collision.h>
#include <Game/Game.h> // this fixes circular dependency for now

bool Collision::at(Location &next) {
   //if(monster.location.world() == next.world() ||
   //   player.location.world() == next.world())
   //   return true;
   return false;
}
