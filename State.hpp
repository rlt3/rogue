#ifndef DEARTH_STATE_HPP
#define DEARTH_STATE_HPP

namespace State {
  enum Direction {
    UP    = 0,
    RIGHT = 1,
    DOWN  = 2,
    LEFT  = 3
  };

  enum Attack {
    UP    = 4,
    RIGHT = 5,
    DOWN  = 6,
    LEFT  = 7
  };

};

#endif
